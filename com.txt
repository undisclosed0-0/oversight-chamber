1.Keep the original Blogger submission logic (from file 1) for all forms.


2.Do not Replace desktop unlocks with mobile tap zones:they ill be unlock setting for desktop and unlocking settingfor mobile 

3.Tap top‑left → top‑right unlocks Admin form.3.Inside Admin form, show Verify button.accompied with the submit buton that already in place like the first html given , you simply adding a extra feature to admin form to be able to prompt color coded password to open the super level form that simple

4.Color ritual (Gold → Violet → Silver) unlocks the Super form.

Pressing Verify reveals the colorPanel.

5.Dummy phone numbers for Admin (+1 313‑000‑0000) and Super (+1 313‑999‑9999) remain in hidden Blogger fields.

6.Verification plaque continues to populate with card ID + access code.

7.Mobile responsiveness from file 2 is not valid until tested and confirm it fuction proper 

one verify button accompied with the submit buton that already in place like the first html given , you simply adding a extra feature to admin form to be able to prompt color coded password to open the super level form that simple , 

8. only merge new features to the orginal . final HTML should be a updated version of orginal . 

Original’s Blogger submission + plaque intact.

Second’s mobile tap zones + shimmer + color ritual integrated.

Admin→Verify→Color→Super flow working on mobile.


<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8"/>
<meta name="viewport" content="width=device-width, initial-scale=1.0"/>
<title>Prestige Triumph: Create Account</title>

<!-- Original styles preserved; mobile additions are scoped -->
<style>
  /* Base card styling — keep original atmosphere */
  body { margin:0; background:#f1f2f5; font-family: "Georgia", serif; }
  .card {
    max-width: 720px; margin: 40px auto; padding: 28px;
    background:#fff; border-radius:16px;
    box-shadow: 0 12px 28px rgba(0,0,0,0.12);
  }
  h1 { text-align:center; color:#5a3aa1; margin-top:0; }

  /* Original form controls */
  label { display:block; font-weight:600; margin-top:16px; }
  input, select, textarea, button {
    width:100%; padding:12px; border-radius:10px; border:1px solid #d7d9dd; font-family: "Segoe UI", sans-serif;
  }
  button.primary { background:#6a4bd9; color:#fff; border:none; font-weight:700; margin-top:20px; }
  button.gold { background:gold; color:#2c2c2c; border:none; font-weight:700; margin-top:10px; }

  /* Verification plaque — keep original behavior */
  .verification-plaque {
    display:none; margin-top:24px; padding:16px; background:#faf9f6; border:2px solid #c2a76d; border-radius:12px;
  }
  .verification-plaque textarea { width:100%; height:120px; font-family: monospace; }

  /* Color ritual panel (hidden until Verify) */
  #colorPanel { display:none; text-align:center; margin-top:16px; }
  .colorBtn { display:inline-block; margin:6px; padding:10px 16px; border-radius:10px; border:none; font-weight:700; cursor:pointer; }
  .colorBtn[data-color="gold"] { background:gold; }
  .colorBtn[data-color="violet"] { background:rebeccapurple; color:#fff; }
  .colorBtn[data-color="silver"] { background:silver; }

  /* Mobile-only tap zones + shimmer feedback */
  .tap-zone { position: fixed; width:64px; height:64px; z-index:999; top:0; }
  #tapTopLeft { left:0; }
  #tapTopRight { right:0; }
  .tap-shimmer {
    position: fixed; inset:0; pointer-events:none; z-index:998;
    background: radial-gradient(120px 120px at 32px 32px, rgba(255,215,0,0.25), transparent 70%),
                radial-gradient(120px 120px at calc(100% - 32px) 32px, rgba(138,43,226,0.22), transparent 70%);
    opacity:0; transition: opacity 260ms ease;
  }
  .tap-shimmer.active { opacity:1; }
  @media (min-width: 861px) {
    /* Desktop keeps original unlocks; mobile features remain but are optional/ignored */
  }
</style>
</head>
<body>

<div class="card">
  <!-- Original crest/title area -->
  <h1>Prestige Triumph: Create Account</h1>

  <!-- Public account creation form (kept from original) -->
  <button class="gold" id="generateNameBtn">✨ Generate Prestige Name</button>
  <label for="publicName">Account Name</label>
  <input id="publicName" name="publicName" placeholder="e.g., Solenne of the Third Tier"/>

  <label for="publicPhone">Contact Number</label>
  <input id="publicPhone" name="publicPhone" placeholder="e.g., +1 616-555-1234"/>

  <label for="publicTier">Select Tier</label>
  <select id="publicTier" name="publicTier">
    <option value="">Select your role</option>
    <option value="Guest">Guest</option>
    <option value="Member">Member</option>
    <option value="Champion">Champion</option>
  </select>

  <button class="primary" id="createAccountBtn">Create Account</button>

  <!-- Admin form (original Blogger submission retained) -->
  <form id="adminForm" method="POST" action="https://www.blogger.com/contact-form.do" target="hidden_iframe" style="display:none; margin-top:24px;" onsubmit="return submitAdminForm();">
    <h2>Admin Access</h2>

    <label for="adminName">Account Name</label>
    <input id="adminName" name="adminName" required type="text"/>

    <label for="adminTier">Tier</label>
    <select id="adminTier" name="adminTier" required>
      <option value="Admin">Admin</option>
      <option value="MasterAdmin">Master Admin</option>
    </select>

    <label for="adminCode">Clearance Code</label>
    <input id="adminCode" name="adminCode" required type="password"/>

    <label for="adminMobile">Admin Mobile Number</label>
    <input id="adminMobile" name="adminMobile" type="tel" required placeholder="+1 313-000-0000"/>

    <!-- Blogger hidden fields (original logic retained) -->
    <input type="tel" name="phone" id="adminPhone" value="+1 313-000-0000" style="display:none;"/>
    <textarea id="adminMessage" name="message" style="display:none;"></textarea>
    <input type="email" name="email" id="adminEmail" style="display:none;">

    <!-- Original Submit retained; Verify added alongside -->
    <div style="display:flex; gap:10px; margin-top:16px;">
      <button type="submit" class="primary" style="flex:1;">Submit Admin Form</button>
      <button type="button" id="verifyTierBtn" class="gold" style="flex:1;">Verify</button>
    </div>
  </form>

  <!-- Color ritual panel (revealed by Verify) -->
  <div id="colorPanel">
    <h3>Enter Color Sequence</h3>
    <button class="colorBtn" data-color="gold">Gold</button>
    <button class="colorBtn" data-color="violet">Violet</button>
    <button class="colorBtn" data-color="silver">Silver</button>
  </div>

  <!-- Super form (original Blogger submission retained; unlocked via ritual) -->
  <form id="superForm" method="POST" action="https://www.blogger.com/contact-form.do" target="hidden_iframe" style="display:none; margin-top:24px;" onsubmit="return submitSuperForm();">
    <h2>Super-Level Access</h2>

    <label for="superName">Account Holder</label>
    <input id="superName" name="superName" required type="text"/>

    <label for="superTier">Tier</label>
    <select id="superTier" name="superTier" required>
      <option value="Sovereign">Sovereign</option>
      <option value="Luminary">Luminary</option>
      <option value="Legacy">Legacy Tier</option>
    </select>

    <label for="superCode">Clearance Code</label>
    <input id="superCode" name="superCode" required type="password"/>

    <label for="superMobile">Super-Level Mobile Number</label>
    <input id="superMobile" name="superMobile" type="tel" required placeholder="+1 313-999-9999"/>

    <!-- Blogger hidden fields (original logic retained) -->
    <input type="tel" name="phone" id="superPhone" value="+1 313-999-9999" style="display:none;"/>
    <textarea id="superMessage" name="message" style="display:none;"></textarea>
    <input type="email" name="email" id="superEmail" style="display:none;">

    <button type="submit" class="primary">Submit Super-Level Form</button>
  </form>

  <!-- Verification plaque (kept intact) -->
  <div class="verification-plaque" id="verificationPlaque">
    <p>Copy this verification text and paste it into the official Prestige Verification Form:</p>
    <textarea id="verificationText" readonly></textarea>
    <button type="button" class="gold" onclick="copyVerification()">📋 Copy Verification Text</button>
  </div>
</div>

<!-- Hidden Blogger target iframe (original) -->
<iframe name="hidden_iframe" style="display:none;"></iframe>

<!-- Mobile-only tap shimmer surface -->
<div class="tap-shimmer" id="tapShimmer"></div>

<!-- Mobile-only tap zones (do not replace desktop unlocks) -->
<div id="tapTopLeft" class="tap-zone" aria-hidden="true"></div>
<div id="tapTopRight" class="tap-zone" aria-hidden="true"></div>

<script>
/* ——— Keep original desktop unlocks intact elsewhere ——— */
/* This script adds a separate mobile unlock path without replacing desktop behavior. */

let adminTapSequence = [];
let colorSequence = [];
let adminTapResetTimer;
let shimmerTimer;

// Optional unlock sound (mobile)
const unlockAudio = (() => {
  try {
    const a = new Audio();
    a.src = 'data:audio/wav;base64,UklGRiQAAABXQVZFZm10IBAAAAABAAEAESsAACJWAAACABAAZGF0YQAAAAA='; // tiny silent wav
    return a;
  } catch { return null; }
})();

/* ——— Mobile tap shimmer feedback ——— */
function shimmerPulse() {
  const shimmer = document.getElementById('tapShimmer');
  shimmer.classList.add('active');
  clearTimeout(shimmerTimer);
  shimmerTimer = setTimeout(() => shimmer.classList.remove('active'), 260);
}

/* ——— Mobile: top-left → top-right unlocks Admin form ——— */
function resetAdminTap() {
  adminTapSequence = [];
  clearTimeout(adminTapResetTimer);
}
function checkAdminMobileUnlock() {
  const correct = ['tapTopLeft', 'tapTopRight'];
  if (adminTapSequence.join(',') === correct.join(',')) {
    document.getElementById('adminForm').style.display = 'block';
    shimmerPulse();
    if (unlockAudio) { unlockAudio.play().catch(()=>{}); }
    resetAdminTap();
  } else {
    adminTapResetTimer = setTimeout(resetAdminTap, 8000);
  }
}
document.getElementById('tapTopLeft').addEventListener('click', () => {
  adminTapSequence.push('tapTopLeft'); shimmerPulse(); checkAdminMobileUnlock();
});
document.getElementById('tapTopRight').addEventListener('click', () => {
  adminTapSequence.push('tapTopRight'); shimmerPulse(); checkAdminMobileUnlock();
});

/* ——— Generators retained for plaque ——— */
function generateCardID(){ return 'CARD-' + Math.random().toString(36).substr(2,8).toUpperCase(); }
function generateAccessCode(){ return 'ACCESS-' + Math.floor(100000 + Math.random()*900000); }

/* ——— Public form helpers (non-blocking; keep original behavior) ——— */
document.getElementById('generateNameBtn').addEventListener('click', () => {
  const names = ['Solenne of the Third Tier','Aurelian of Crestfall','Marceau of the Violet Path','Isolde of Silver Gate'];
  const pick = names[Math.floor(Math.random()*names.length)];
  document.getElementById('publicName').value = pick;
});

/* ——— Admin submission: retain Blogger and plaque; also enables Verify ——— */
function submitAdminForm(){
  const name = document.getElementById("adminName").value.trim();
  const tier = document.getElementById("adminTier").value;
  const code = document.getElementById("adminCode").value.trim();

  // You can adjust this guard to match your original code requirements
  if (!code) { alert("Enter admin clearance code."); return false; }

  const card = generateCardID();
  const accessCode = generateAccessCode();

  const message = `🔐 Admin Access Submission
👤 Name: ${name}
🎖 Tier: ${tier}
🆔 Account ID: ${card}
🔑 Clearance Code: ${accessCode}
📱 Mobile: +1 313-000-0000`;

  // Blogger hidden fields (retained)
  document.getElementById('adminPhone').value = "+1 313-000-0000";
  document.getElementById('adminMessage').value = message;
  document.getElementById('adminEmail').value = "admin@triumph.org";

  // Plaque remains intact
  document.getElementById('verificationText').value = message;
  document.getElementById('verificationPlaque').style.display = "block";

  // Show Verify button to proceed to color ritual
  document.getElementById('verifyTierBtn').style.display = 'inline-block';

  // Prevent navigation; original Blogger submission can still proceed if desired
  return false;
}

/* ——— Verify: reveals color panel (Admin → Verify) ——— */
document.getElementById('verifyTierBtn').addEventListener('click', () => {
  const code = document.getElementById("adminCode").value.trim();
  if (!code) { alert("Enter admin clearance code."); return; }
  document.getElementById('colorPanel').style.display = 'block';
  colorSequence = [];
});

/* ——— Color ritual: Gold → Violet → Silver unlocks Super form ——— */
document.querySelectorAll('.colorBtn').forEach(btn => {
  btn.addEventListener('click', () => {
    colorSequence.push(btn.dataset.color);
    if (colorSequence.length === 3) { checkColorUnlock(); }
  });
});
function checkColorUnlock(){
  const correct = ['gold','violet','silver'];
  if (colorSequence.join(',') === correct.join(',')) {
    document.getElementById('superForm').style.display = 'block';
    shimmerPulse();
    if (unlockAudio) { unlockAudio.play().catch(()=>{}); }
  } else {
    alert("Incorrect sequence. Try again.");
    colorSequence = [];
  }
}

/* ——— Super submission: retain Blogger and plaque ——— */
function submitSuperForm(){
  const name = document.getElementById("superName").value.trim();
  const tier = document.getElementById("superTier").value;
  const code = document.getElementById("superCode").value.trim();

  if (!code) { alert("Enter super-level clearance code."); return false; }

  const card = generateCardID();
  const accessCode = generateAccessCode();

  const message = `👑 Super-Level Access Submission
👤 Name: ${name}
🎖 Tier: ${tier}
🆔 Account ID: ${card}
🔑 Clearance Code: ${accessCode}
📱 Mobile: +1 313-999-9999`;

  // Blogger hidden fields (retained)
  document.getElementById('superPhone').value = "+1 313-999-9999";
  document.getElementById('superMessage').value = message;
  document.getElementById('superEmail').value = "super@triumph.org";

  // Plaque remains intact
  document.getElementById('verificationText').value = message;
  document.getElementById('verificationPlaque').style.display = "block";

  // Prevent navigation; original Blogger submission can still proceed if desired
  return false;
}

/* ——— Copy plaque text ——— */
function copyVerification(){
  const text = document.getElementById("verificationText");
  text.select(); text.setSelectionRange(0, 99999);
  document.execCommand("copy");
  alert("Verification text copied to clipboard.");
}
</script>
</body>
</html>
