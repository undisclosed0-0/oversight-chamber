<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Unlock the Chamber</title>
  <style>
    body {
      background: radial-gradient(circle at center, #0b0b0b, #000);
      color: #ffd700;
      font-family: 'Orbitron', monospace;
      text-align: center;
      padding: 60px 20px;
    }
    h1 {
      font-size: 1.8em;
      text-shadow: 0 0 12px rgba(255,215,0,0.6);
    }
    input {
      padding: 12px;
      width: 80%;
      max-width: 300px;
      font-size: 1em;
      border: 2px solid #444;
      background: #111;
      color: #ffd700;
      border-radius: 6px;
      margin-top: 20px;
    }
    button {
      margin-top: 20px;
      padding: 10px 20px;
      font-weight: bold;
      background: linear-gradient(#ffd700, #b8860b);
      border: 2px solid #222;
      color: #111;
      border-radius: 8px;
      cursor: pointer;
      box-shadow: 0 0 10px rgba(255,215,0,0.5);
    }
    .sigil {
      font-size: 3em;
      margin: 20px 0;
      text-shadow: 0 0 20px rgba(255,215,0,0.4);
    }
  </style>
</head>
<body>
  <div class="sigil">⚚ ⟁ ♛</div>
  <h1>Enter the Ritual Phrase</h1>
  <input type="text" id="ritualPhrase" placeholder="e.g. TAP TAP CROWN" />
  <br />
  <button onclick="unlockChamber()">Unlock</button>

  <script>
    function unlockChamber() {
      const phrase = document.getElementById('ritualPhrase').value.trim().toUpperCase();
      if (phrase === 'TAP TAP CROWN') {
        localStorage.setItem('adminUnlocked', 'true');
        window.location.href = 'index.html';
      } else {
        alert('Incorrect phrase. Try again.');
      }
    }
  </script>
</body>
</html>
